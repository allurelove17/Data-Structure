//#include "permutation.hpp"

/*template<class T>
void Permutation<class T>::permutation(int n) {
	std::ofstream ofs;
	ofs.open("test.txt");
	if (!ofs.is_open()) {
		std::cout << "Failed to open file!\n";
	}
	for (int i = 0; i < n; ++i) {
		int x = rand() % INT_MAX;
		ofs << x << ' ';
	}
	ofs.close();
}*/
